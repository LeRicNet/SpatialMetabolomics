% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateMetabolicScores.R
\name{calculateMetabolicScores,SpatialMetabolic-method}
\alias{calculateMetabolicScores,SpatialMetabolic-method}
\title{Calculate metabolic pathway scores}
\usage{
\S4method{calculateMetabolicScores}{SpatialMetabolic}(
  object,
  pathways = c("oxphos", "glycolysis", "mito_biogenesis", "mito_dynamics"),
  method = "seurat",
  scale = TRUE,
  nbin = 24,
  ctrl = 100,
  seed = 42,
  BPPARAM = SerialParam()
)
}
\arguments{
\item{object}{SpatialMetabolic object}

\item{pathways}{List of pathway gene sets or pathway names}

\item{method}{Scoring method ("seurat", "ssgsea", "aucell", "mean")}

\item{scale}{Whether to scale scores (default: TRUE)}

\item{nbin}{Number of bins for Seurat scoring (default: 24)}

\item{ctrl}{Number of control genes for Seurat method (default: 100)}

\item{seed}{Random seed for reproducibility}

\item{BPPARAM}{BiocParallel parameters}
}
\value{
SpatialMetabolic object with metabolic scores
}
\description{
Calculate metabolic pathway scores
}
\examples{
\dontrun{
# Calculate scores for default pathways
spm <- calculateMetabolicScores(spm)

# Calculate scores for specific pathways
spm <- calculateMetabolicScores(spm,
                               pathways = c("oxphos", "glycolysis"),
                               method = "seurat")

# Use custom gene sets
custom_pathways <- list(
    MyPathway1 = c("Gene1", "Gene2", "Gene3"),
    MyPathway2 = c("Gene4", "Gene5", "Gene6")
)
spm <- calculateMetabolicScores(spm, pathways = custom_pathways)
}
}
